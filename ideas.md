# Ideas

## Find Mode

- Add a `//find` feature that works similarly to `Ctrl+F`.
- Use `Esc` to exit find mode.
- Use arrow keys to move between matches.
- Consider highlighting the active match and showing total match count.

## Code Box Language Support

- Add Java support to the code box.
- Check whether syntax highlighting, formatting, and language labels need updates.

## Billing System

- Use Supabase as the database layer for a server-side billing system.